@model Financeiro.Models.bancos

@{
    ViewBag.Title = "Details";
}

<div>
    <h6>
        Detalhes do Banco <button type="button" class="close" data-dismiss="modal" aria-label="Close" style="color:crimson;">
            <span aria-hidden="true">&times;</span>
        </button>
    </h6>

    <hr class="corpadrao" />
    <div style="font-size:small">
        <div class="">
            <b>@Html.DisplayNameFor(model => model.descricao):</b>
            @Html.DisplayFor(model => model.descricao)
        </div>
    </div>
</div>
<hr class="corpadrao" />
<div align="right">
    <a class="btn btn-outline-primary btn-sm" href="@Url.Action("Edit","bancos_contas",new { id = Model.bancos_id })" data-toggle="tooltip" data-placement="top" title="Editar"><span class="fa fa-pencil"></span>Editar</a>
</div>
